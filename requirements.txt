# Self-Service Printing Kiosk — Python dependencies
# Install with: pip install -r requirements.txt
#
# System-level dependencies (install separately):
#   sudo apt-get install cups libcups2-dev poppler-utils
#   sudo systemctl start cups
#   sudo systemctl start pigpiod
#
# pigpio (Raspberry Pi only):
#   pip install pigpio   (Linux/Pi only; skipped automatically via SIM_MODE on laptops)

# GUI
PyQt5>=5.15

# PDF handling
PyMuPDF>=1.23
pdf2image>=1.16

# Image analysis
opencv-python>=4.8
numpy>=1.24

# Hardware communication (GSM modem)
pyserial>=3.5

# Web backend (FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.29
cryptography>=42.0
python-multipart>=0.0.9  # required for FastAPI's UploadFile/Form parsing (webapp/routers/upload.py)

# Session manager (OTP + QR intake sessions)
qrcode[pil]>=7.4

# Testing & dev tooling
pytest>=7.4
flake8>=6.0
httpx>=0.27

# Optional dependencies (for Windows only)
psutil
pywin32
